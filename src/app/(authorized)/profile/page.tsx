import { ComingSoonView } from '@/views/coming-soon';

const ProfilePage = () => (
	<ComingSoonView
		title='Профиль'
		description='Личные настройки, нотификации, токены доступа.'
	/>
);

export default ProfilePage;
